#ifndef _MULTIPLY_
#define _MULTIPLY_

#include "main.h"

void transpose(double matrix[M][N], double transposed[N][M]);

void matrix_multiplication(double matrix1[N][M], double matrix2[M][P], double result_matrix[N][P]);

#endif /*_MULTIPLY_*/
